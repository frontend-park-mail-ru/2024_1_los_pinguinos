  <div class="header">
    <div class="header__logo">
    </div>
      <button type="button" id="header__button{{#if authState}}--logout{{/if}}">{{#if authState}}Выйти{{/if}}{{#if notAuthState}}Войти{{/if}}</button>
  </div>