import MatchesList from '../../widgets/matchesList/index';

const MatchesPage = () => {
    return <div
    style={{
        height: '100%',
    }}
    >{MatchesList()}</div>;
};

export default MatchesPage;
