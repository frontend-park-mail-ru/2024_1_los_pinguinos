import LoginForm from '../../features/loginform/index';

export const Login = () => {
    return (
        <div>
            <div className="wrapper">
                <LoginForm />
            </div>
        </div>
    );
};

export default Login;
